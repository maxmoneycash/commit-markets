# Harvested data is derived from real (incl. private) repos — never commit it.
/data/*
!/data/.gitkeep

# Generated charts
*.png

# Rust build output
/target/

# Python (legacy)
__pycache__/
*.pyc
.venv/

.DS_Store

# internal docs never go in the public repo — keep them in commit-markets-internal
/docs/internal/
.vercel
