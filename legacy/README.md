# Legacy Python/bash prototype

Superseded by the Rust `cmkt` binary (see repo root). Kept only because the
matplotlib chart renderer in `aggregate.py` has not been ported yet. Not the
system of record. See `docs/DECISION.md` for the rationale.
